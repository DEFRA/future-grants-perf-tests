# Entra ID (Microsoft) Credentials
# These are required for authentication
ENTRA_ID_USERNAME=your-email@example.com
ENTRA_ID_PASSWORD=your-password

# Alternative variable names (if you prefer these)
# ENTRA_ID_ADMIN_USER=your-email@example.com
# ENTRA_ID_USER_PASSWORD=your-password

# Application URL
# The URL where your application is hosted (initial login page)
APP_URL=https://fg-cw-frontend.perf-test.cdp-int.defra.cloud/

# Expected Domain
# The domain name expected after successful login (used for verification)
EXPECTED_DOMAIN=fg-cw-frontend

# Cookie Name
# The name of the session cookie your application sets
COOKIE_NAME=CW-FE

# Cases URL
# URL to navigate after login to force session creation
CASES_URL=https://fg-cw-frontend.perf-test.cdp-int.defra.cloud/cases

# Case HREF
# Specific case to click on to ensure full session is established
CASE_HREF=/cases/695cefcf55689bcb16d66afa

# Output File
# Path where the session cookie JSON will be saved for JMeter
OUTPUT_FILE=./artifacts/cw-fe-session.json

# Debug Mode
# Set to 'true' to run browser in non-headless mode with debug output
# DEBUG=false
